# Optimized requirements for smaller Docker image
# Only essential packages for production

# Core web framework
flask==3.1.3
flask-cors==6.0.2
flask-socketio==5.6.1

# WSGI server (smaller than gunicorn)
waitress==3.0.0

# Database
psycopg2-binary==2.9.9

# Security
werkzeug==3.1.7
cryptography==46.0.6

# Utilities
python-dotenv==1.0.0

# Remove development and heavy packages:
# - gunicorn (use waitress instead)
# - redis (not needed for basic deployment)
# - blinker, itsdangerous (flask includes them)
# - pyjwt (using simple auth for demo)
# - python-jose (not needed for simple auth)
